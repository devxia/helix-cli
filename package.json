{
  "name": "helix",
  "version": "0.2.1",
  "type": "module",
  "scripts": {
    "dev": "bun --hot src/main.ts",
    "build": "bun build --compile --outfile helix src/main.ts",
    "test": "bun test",
    "test:integration": "bun test ./test/integration/seqkit.integration.ts",
    "typecheck": "bunx tsc --noEmit"
  },
  "dependencies": {
    "@earendil-works/pi-ai": "0.82.1",
    "@earendil-works/pi-coding-agent": "0.82.1",
    "typebox": "1.1.38"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^6.0.3"
  }
}
